# Data Model

*Note: As this is a base frontend template, it does not define a comprehensive domain data model. An example entity is provided to demonstrate the architecture.*

## Example Entity (User)

```typescript
// src/features/example/types/index.ts
export type User = {
  id: string;
  email: string;
  name: string;
  createdAt: string;
};
```
